{
  "name": "beam.byronwade.com",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.2.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "convex"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "dev:web": "turbo run dev --filter=@beam/web",
    "dev:convex": "turbo run dev --filter=@beam/convex"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "convex": "^1.30.0"
  }
}
