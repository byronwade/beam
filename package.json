{
  "name": "beam.byronwade.com",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.2.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "dev:web": "turbo run dev --filter=@byronwade/beam-web"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}